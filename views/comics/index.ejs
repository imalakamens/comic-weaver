<%- include('../partials/header') %> 
    <div id="mid">
        <h1><%= title %></h1>
        <p>💥Aite bet here is where all comics will be displayed💥</p>
    </div>
    <div>
        <% comics.forEach( c => { %>
        <div><%= c.title %>
            <a href="comics/<%=c._id%>">
                detail view 
            </a>
            <form style="display: inline;" action="/comics/<%=c._id%>?_method=DELETE " method="POST"> <!-- finna be a delete action here -->
                <button class="btn btn-danger btn-sm">Delete Comic</button>
                </form>
        </div>
        <% }); %>      
    </div>

<%- include('../partials/footer') %> 